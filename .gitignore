# secrets / credentials — never commit
.env
.env.*
*.pem
*.key
credentials.json

# benchmark raw runs — local only; commit only RESULTS.md + chart.svg
benchmarks/runs/

# python / os scratch
__pycache__/
*.pyc
.DS_Store

# local agent state
.claude/
