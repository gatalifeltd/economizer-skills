# Benchmark Results

_Not run yet._ Regenerate with:

```bash
cursor-agent login        # one-time, for the cursor arm
bash run.sh               # tasks × {with,without} × reps × agents
python3 analyze.py        # → this file + chart.svg
```

See [`README.md`](README.md) for methodology and limitations.
